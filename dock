version: '3'

services:
  portfolio:
    image: my-react-portfolio:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: always
